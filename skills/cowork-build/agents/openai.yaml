interface:
  display_name: "Cowork: Build"
  short_description: "Let Claude build while Codex verifies."
  default_prompt: "Freeze and build the plan with Claude, then independently verify the result."
